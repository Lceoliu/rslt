train:
  epochs: 3
  log_interval: 10
  val_interval: 50
  save_dir: runs/ckpts

data:
  batch_size: 8
  num_workers: 0
  T: 32
  train_length: 256
  val_length: 128
  # custom_builder: your_module:your_builder  # should return (train_loader, val_loader, nclass)

nclass: 10
